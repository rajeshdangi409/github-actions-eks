apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: apps
  namespace: flux-system

spec:
  interval: 1m

  path: ./clusters/dev

  prune: true

  wait: true

  timeout: 5m

  sourceRef:
    kind: GitRepository
    name: github-actions-eks